import { emptyRuntime } from '../helpers.js'

export default {
  type: 'TSaveDialog',
  definition: { kind: 'nonvisual', w: 32, h: 32, prefix: 'SaveDialog', icon: '💾', cat: 'Dialogs', nonvisual: true },
  properties: [],
  renderRuntime: emptyRuntime,
}
