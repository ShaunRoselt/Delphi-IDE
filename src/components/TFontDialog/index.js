import { emptyRuntime } from '../helpers.js'

export default {
  type: 'TFontDialog',
  definition: { kind: 'nonvisual', w: 32, h: 32, prefix: 'FontDialog', icon: 'F', cat: 'Dialogs', nonvisual: true },
  properties: [],
  renderRuntime: emptyRuntime,
}
